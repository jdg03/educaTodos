<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Matricula Instituto</title>
    <link rel="stylesheet" href="../css/styleheader.css">
    <link rel="stylesheet" href="../css/matriculainstituto.css">
</head>

<body class="fondo">
    <!-- sección menu -->
    <%-include("./partials/header") %>
        <!-- sección Bienvenida -->
        <div class="bienvenida">
            <h2>Matricula</h2>
        </div>
        <hr>
        <!-- sección informacion instituto -->
        <div class="instituto">
            <div class="nombre">
                <h1>
                    <%=institucion.nombre%>
                </h1>
            </div>


            <div class="info">
                <h2>Información Académica</h2>
                <div class="grados">
                    <h3>Seleccione la seccion que deseas matricular</h3>
                    <select name="grado" id="grado">
                        <% secciones.forEach(seccion=> { %>
                            <option value="<%= seccion.id %>">
                               Grado: <%= seccion.grado %> Seccion: <%= seccion.seccion %> Jornada:  <%= seccion.jornada %>
                            </option>
                            <% }); %>
                    </select>


                </div>
            </div>
            <div class="regresar">
                <a href="">
                    <button class="button1" type="submit">Confirmar matricula</button>
                </a>
            </div>
        </div>



        </div>


        <script src="../js/main.js"></script>
        <%-include("./partials/footer") %>